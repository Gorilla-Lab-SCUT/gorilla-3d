###################
_base_: ./common.yaml

## Model options
model:
  type: SalsaNext
  nclasses: 20

dataloader:
  batch_size: 8
  num_workers: 16

solver:
  # train mode
  epochs: 150
  save_freq: 4599  # also eval_freq
